<div>
    <%= form_for @room do |form| %>
    <div class="row mt-2">
        <%= form.text_field :name, placeholder: "Enter room name..." %>
    </div>

    <div>
        <%= form.select(:name, User.all.collect {|p| [ p.id ] },
                                        { :prompt => "Please select"},
                                        { :multiple => true, :size => 5 }) %>
        S
    </div>

    <div class="row mt-2">
        <%= form.submit "Save", class: 'btn btn-success' %>
    </div>

    <% end %>
</div>